# Modpack-Blacklist
A tiny bash script, to automatically tweak any modpack. It removes blacklisted mods, adds missing mods from a wishlist, auto-injects settings/keybinds, and forces configs that all overwrite whatever a modpack comes with.
